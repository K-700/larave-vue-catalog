import { RootMutationType } from "../root/RootMutationType";
import { SearchMutationType } from "../search";

export namespace MutationType {
    export const root = RootMutationType
    export const search = SearchMutationType
}
